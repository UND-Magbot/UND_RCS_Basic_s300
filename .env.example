# 이 파일을 .env로 복사 후 값 수정
# cp .env.example .env

# ── 데이터베이스 ──────────────────────────────
# 이중화 시 VIP(가상 IP) 주소 사용
DB_HOST=192.168.10.5
DB_PORT=3306
DB_USER=root
DB_PASSWORD=unde5466
DB_NAME=rcs_db

# ── 프론트엔드 API URL ────────────────────────
# 서버 IP에 맞게 변경
NEXT_PUBLIC_API_URL=http://서버IP:8003
